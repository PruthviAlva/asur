# .github/workflows/ci.yml
name: Asur CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [main]

jobs:
  # ── Lint + Build Check (client) ─────────────────
  client-build:
    name: Client Build Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: client/package-lock.json

      - name: Install client deps
        working-directory: client
        run: npm ci

      - name: Build client
        working-directory: client
        run: npm run build
        env:
          VITE_API_URL: https://asur-1n3w.onrender.com/api
          VITE_GOOGLE_CLIENT_ID: placeholder

  # ── Server Check ────────────────────────────────
  server-check:
    name: Server Dependency Check
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: server/package-lock.json

      - name: Install server deps
        working-directory: server
        run: npm ci

      - name: Generate Prisma client
        working-directory: server
        run: npx prisma generate
        env:
          DATABASE_URL: postgresql://asurTv:npg_gY9MwryeUq2d@ep-shy-bonus-anrzdia9-pooler.c-6.us-east-1.aws.neon.tech/asurTv?sslmode=require&channel_binding=require
